profile: production
exclude_paths:
  - .ansible/
  - .github/
  - .venv/
  - backstage/
  - gitops/
  - terraform/

skip_list:
  # The bootstrap workflow intentionally installs tools onto a local Debian host.
  - package-latest

warn_list:
  - experimental
