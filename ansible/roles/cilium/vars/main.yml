---

# Gateway API CRD name for verification
cilium_gateway_api_gatewayclass_crd: gatewayclasses.gateway.networking.k8s.io

# Cilium resources to verify
cilium_daemonset: cilium
cilium_operator_deployment: cilium-operator
