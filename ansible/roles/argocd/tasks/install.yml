---
- name: Install ArgoCD via Helm (via OCI)
  kubernetes.core.helm:
    name: argocd
    chart_ref: oci://ghcr.io/argoproj/argo-helm/argo-cd
    chart_version: 9.3.7
    release_namespace: "{{ argocd_namespace }}"
    create_namespace: true
    values_files:
      - "{{ playbook_dir }}/../../gitops/infrastructure/argocd/values.yaml"
  environment:
    KUBECONFIG: "{{ k3s_kubeconfig }}"
  tags: [k3s, argocd]

- name: Wait for ArgoCD to be ready
  ansible.builtin.command: k3s kubectl -n "{{ argocd_namespace }}" rollout status deployment/argocd-server --timeout=300s
  changed_when: false
  tags: [k3s, argocd]

- name: Display ArgoCD installation status
  ansible.builtin.debug:
    msg: "✓ ArgoCD installed in {{ argocd_namespace }} namespace"
  tags: install
