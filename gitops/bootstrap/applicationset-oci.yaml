apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: oci-apps
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/nunoferna/homelab.git
        revision: main
        files:
          - path: "gitops/**/app.yaml"
        pathParamPrefix: src
      selector:
        matchExpressions:
          - { key: repoURL, operator: Exists }
          - { key: chart, operator: DoesNotExist }
          - { key: noBase, operator: DoesNotExist }
  template:
    metadata:
      name: "{{.appName}}"
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: '{{ default "0" .syncWave }}'
    spec:
      project: '{{ index (splitList "/" .src.path.path) 1 }}'
      sources:
        - repoURL: "{{.repoURL}}"
          targetRevision: "{{.targetRevision}}"
          path: "."
          helm:
            valueFiles:
              - "$values/{{.src.path.path}}/values.yaml"
        - repoURL: https://github.com/nunoferna/homelab.git
          targetRevision: main
          ref: values
        - repoURL: https://github.com/nunoferna/homelab.git
          targetRevision: main
          path: "{{.src.path.path}}/base"
          directory:
            recurse: true
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply={{ default "false" .serverSideApply }}

---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: oci-apps-nobase
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/nunoferna/homelab.git
        revision: main
        files:
          - path: "gitops/**/app.yaml"
        pathParamPrefix: src
      selector:
        matchExpressions:
          - { key: repoURL, operator: Exists }
          - { key: chart, operator: DoesNotExist }
          - { key: noBase, operator: Exists }
  template:
    metadata:
      name: "{{.appName}}"
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: '{{ default "0" .syncWave }}'
    spec:
      project: '{{ index (splitList "/" .src.path.path) 1 }}'
      sources:
        - repoURL: "{{.repoURL}}"
          targetRevision: "{{.targetRevision}}"
          path: "."
          helm:
            valueFiles:
              - "$values/{{.src.path.path}}/values.yaml"
        - repoURL: https://github.com/nunoferna/homelab.git
          targetRevision: main
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply={{ default "false" .serverSideApply }}
