name: Layer 1 - Host Bootstrap

on:
  push:
    branches: [ "main" ]
    paths:
      - 'bootstrap/**'
  workflow_dispatch:

jobs:
  bootstrap-host:
    runs-on: self-hosted
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Host Bootstrap
        run: |
          cd bootstrap
          chmod +x main.sh
          ./main.sh
